apiVersion: apps/v1
kind: Deployment
metadata:
  name: dq-dashboard
  namespace: dq-ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dq-dashboard
  template:
    metadata:
      labels:
        app: dq-dashboard
    spec:
      containers:
        - name: dq-dashboard
          image: lmaofrankie96/dq-dashboard:latest
          ports:
            - containerPort: 8501
          envFrom:
            - configMapRef:
                name: dq-config
          env:
            - name: ORCHESTRATOR_API_URL
              value: "http://orchestrator-api:8002"
            - name: LLM_API_URL
              value: "http://llm-gemini:8001"
          readinessProbe:
            httpGet:
              path: /
              port: 8501
            initialDelaySeconds: 5
            periodSeconds: 5
